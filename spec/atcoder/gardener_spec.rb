# frozen_string_literal: true

RSpec.describe Atcoder::Gardener do
  it "has a version number" do
    expect(Atcoder::Gardener::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
